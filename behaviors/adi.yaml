---
bundle:
  name: adi-behavior
  version: 0.2.0
  description: >
    Thin wiring for ADI's invoke-then-load surface. Registers the two agents
    (adi-orchestrator, quality-gate) as cheap name+description metadata only —
    their heavy tool/sub-bundle/context composition lives in the agent files
    themselves and loads only when spawned via delegate. Also wires the
    /adi skill so it is discoverable as a slash command.

agents:
  include:
    - adi:adi-orchestrator
    - adi:quality-gate

tools:
  # Point the skills tool at ADI's own skills/ directory so the /adi front door
  # is discovered. This mirrors design-council's behavior exactly: bundle.md
  # includes the full amplifier-bundle-skills bundle (which brings the tool AND
  # the curated collection), and this behavior adds only "@adi:skills" on top.
  # The two tool-skills declarations merge by module ID; the curated collection
  # comes from the top-level include, ADI's own skill comes from here.
  - module: tool-skills
    source: git+https://github.com/microsoft/amplifier-bundle-skills@main#subdirectory=modules/tool-skills
    config:
      skills:
        - "@adi:skills"
